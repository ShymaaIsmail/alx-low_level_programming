#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 *_strlen - length of string
 *@s: pointer to char
 *Return: int
 */

int _strlen(char *s)
{
	int length = strlen(s);

	return (length);
}

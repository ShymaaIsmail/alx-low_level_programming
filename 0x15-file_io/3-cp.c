#include "main.h"

/**
 * main - check the code
 * @ac: ac
 * @av: av
 * Return: Always 0.
 */
int main(int ac, char **av)
{
int result = 0;

if (ac != 3)
{
dprintf(1, "Usage: cp file_from file_to");
exit(97);
}
return (result);
}
